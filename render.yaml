
services:
  - type: web
    name: fraud-detection-demo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run fraud_app.py --server.port $PORT --server.address 0.0.0.0
